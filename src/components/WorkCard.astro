---
import type { CollectionEntry } from "astro:content";
import { categoryLabel } from "../data/categories";

interface Props {
  entry: CollectionEntry<"works">;
  index: number;
}

const { entry, index } = Astro.props;
const { title, category, overview, year, cover } = entry.data;
const num = String(index + 1).padStart(2, "0");
---

<a class="card" href={`/works/${entry.id}/`} data-category={category} data-tilt>
  <span class="card__glow" aria-hidden="true" data-tilt-glow></span>

  <span class="card__index mono">
    <span>({num}) — {categoryLabel(category)}</span>
    {year && <span class="card__meta">{year}</span>}
  </span>

  {cover && <img class="card__thumb" src={cover} alt="" loading="lazy" decoding="async" />}

  <div class="card__body">
    <h3 class="card__title">{title}</h3>
    <p class="card__desc">{overview}</p>
    <span class="card__cta mono">
      View project
      <span class="arrow" aria-hidden="true">↗</span>
    </span>
  </div>
</a>
